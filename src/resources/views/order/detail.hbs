<div class="border pt-2">
    <h1 class="text-center mb-4">Order Detail List</h1>
    <table id="tablecategory" class="table table-striped table-bordered" cellspacing="0" width="100%">
        <thead
            class="justify-content-md-between justify-content-sm-center align-content-center border-bottom border-2 my-2 bg-dark text-light p-3 rounded-3">
            <tr class="text-center">
                <th>
                    <strong>No.</strong>
                </th>
                <th>
                    <strong>Product Name</strong>
                </th>
                <th>
                    <strong>Price</strong>
                </th>
                <th>
                    <strong>Quantity</strong>
                </th>
                <th>
                    <strong>Total</strong>
                </th>
                <th>
                    <strong>Image</strong>
                </th>
            </tr>
        </thead>

        <tbody
            class="justify-content-md-between justify-content-sm-center border-bottom border-2 my-2 bg-light p-2 rounded-3">
            {{#each orderDetails}}
            <tr class="text-center">
                <td class="align-middle">
                    {{ sum @index 1 }}
                </td>
                <td class="align-middle">{{ this.name }}</td>
                <td class="align-middle">{{ this.price}}</td>
                <td class="align-middle">{{ this.quantity }}</td>
                <td class="align-middle">{{ this.total_price }}</td>
                <td class="align-middle">
                    <a href="">
                        <img src="/products/{{ this.image }}" border='0' width="70" height="70" />
                    </a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <div class="text-center mb-3">
        <a href="/manage/order" class="btn btn-info">Back to Order</a>
    </div>
</div>